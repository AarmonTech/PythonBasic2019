import matplotlib.pyplot as plt
import numpy as np
"""
x = [ i for i in np.linspace(-4,4,100)] + [ i for i in np.linspace(-4,4,100)]

y = [ np.sqrt( (4 - i**2) ) for i in x] + [ -np.sqrt( (4 - i**2 )) for i in x]

plt.scatter(x + x,y,color="green")
plt.show()
"""
